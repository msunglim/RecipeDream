import {View} from 'react-native';
import {Divider} from 'react-native-paper';

export function ExcuseMeVertically() {
  return (
    <View
      style={{
        height: 20,
        justifyContent: 'center',
      }}>
      <Divider />
    </View>
  );
}
